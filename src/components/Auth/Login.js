import React, { Component } from 'react';
import { Form, Row, Col } from "react-bootstrap";

const Login = (props) => {
    return (
        <div>
            login
        </div>
    )
}

export default Login